@model skjatextar.Models.IndexViewModel

@{
    ViewBag.Title = "Forsíða";
}

<div class="table-header-home">
    <span></span>
</div>

@using (Html.BeginForm("Videos", "Video", @FormMethod.Get, new { @class = "form-search" }))
{
    <div class ="input-append">
        
        @Html.TextBox("LeitarStrengur", null, new { @class = "span2 search-query", @type = "text" })
        <button type="submit" class="submitTakki">Leita</button>
        
        </div>
}

<div class="tableCentered">
    <div class="content-home table-right">
        <table class="table table-condensed table-responsive table-borderedTop" id="FrontPage">
            <thead>
                <tr>
                    <th class="translations">Top 10</th>
                    <th>Atkvæði</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Top10)
                {
                    <tr>
                        <td>@Html.ActionLink(item.Title, "ViewTranslation", new { id = item.ID })</td>
                        <td>@item.LikeCount</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="content-home">
        <table class="table table-condensed table-responsive table-borderedNewest" id="FronPageright">
            <thead>
                <tr>
                    <th class="translations">Nýjustu 10</th>
                    <th>Síðast breytt</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Newest10)
                {
                    <tr>
                        <td>@Html.ActionLink(item.Title, "ViewTranslation", new { id = item.ID })</td>
                        <td>@item.DateLastEdited.ToString("dd.MM.yyyy.HH:mm")</td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>